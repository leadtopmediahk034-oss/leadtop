# 成功案例区标题与高度调整结果

日期：2026-09-10。

## 修改

- 桌面标题恢复到普通文档流，宽度与相邻首页板块共用 `--max: 1680px` 内容边界。
- `#proof` 的桌面锚点避让恢复为 102px，标题不再进入固定导航覆盖区域。
- 取消桌面 `min-height: 100dvh`，案例区改为内容自然撑高。
- 桌面主卡宽度调整为 560–640px，图片高度调整为 250–300px；矮屏图片为 230px。
- 主卡正文、指标及底部切换控件保持可见，两侧倾斜卡片继续保留。

## 验证

- 桌面浏览器实际 CSS 视口为 1800×1125：案例区高 731.26px，区块顶部 102.04px；固定导航底部 86px，标题顶部 158.04px，无重叠。
- 标题左边界 60px，固定导航宽 1680px、左边界 60px，内容边界一致。
- 主卡 640×517.57px，底部 719.31px；切换控件底部 785.30px；均位于案例区底部 833.30px 之前。
- 桌面及移动端 DOM 检查均无横向溢出。
- `git diff --check` 通过。

## 备份与边界

- 修改前备份：`/Users/qianwenyi/Desktop/leadtop-backups/20260910-stories-compact-header/`；复制其中 JSX 与 CSS 回 `components/` 可恢复。
- 备份保留，不提交。
- 本文只记录布局结果，不代表搜索或业务增长改善。
