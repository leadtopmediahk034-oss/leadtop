# 案例列表页实施准备

日期：2026-09-09。当前进入 SOP 阶段 9（页面与内容建设）准备状态，适用 Gate 2。用户希望新增一个类似 https://www.meetsocial.com/cases 的案例页面。

## 已确认的产品与页面上下文

- 当前项目是 Next.js App Router；已有首页、`/polaris`、`/helios`、服务详情、博客和关于/联系页面。
- 首页成功案例轮播已有 3 条案例数据：精密制造企业、智能家居品牌、户外生活方式品牌；当前为模拟案例，指标带有“模拟”标记。
- 已有真实可用的详情入口 `/polaris` 与 `/helios`；第三条案例暂无独立详情页。
- 项目已有统一 Leadtop 导航、字体、配色、按钮、卡片和响应式样式模式；新页面应复用，不重写全站导航。
- 参考页面/用户截图表达的结构：顶部导航；左上 CASES、Success Stories、成功案例；筛选/分类入口；较大案例卡片网格或纵向列表；每卡图片、地区/行业标签、标题、摘要和详情入口；移动端单列；页脚 CTA。

## 需要补齐或明确的事实

- 当前没有经核验的客户名单、Logo、真实成果数值、授权案例图片或完整案例正文。实现阶段只能复用现有模拟数据，并保持“模拟”标记；不可把模拟指标写成真实证明。
- “更多案例”页 URL 暂拟为 `/cases`；三条卡片中第三条可先使用首页轮播展示，详情按钮回到 `#proof` 或进入联系表单，需在实现时按现有链接边界处理。
- 分类筛选可先做前端可访问筛选（全部 / B2B / DTC / 品牌出海），无需新增外部 CMS 或 API；若需真实分页、搜索、后台维护，属于后续范围。

## 拟实施范围（待授权）

1. 新增 `app/cases/page.jsx`，设置页面 title/description，并渲染案例索引页。
2. 新增与页面同域的模块样式（建议 `app/cases/Cases.module.css`），复用现有 CSS 变量、导航组件模式、Phosphor 图标和 Next Image。
3. 将案例数据抽成可复用的本地数据模块，或以最小重复方式从首页迁移；保留现有首页功能，避免未授权的大范围重构。
4. 实现可键盘操作的分类筛选、案例卡片链接、移动端单列和桌面响应式网格；筛选状态不依赖 URL，避免新增索引 URL 与重复页面。
5. 增加页脚前的咨询 CTA，指向现有 `/contactus` 或首页 `#diagnosis`，不提交表单、不传输数据。
6. 不修改首页案例区的当前视觉、不修改详情页内容、不上传外部系统、不添加真实客户或未经来源支持的成果。

## 验收标准

- `/cases` 在本地预览可访问，导航、页脚和返回首页路径正常。
- 桌面 1440×900：标题、分类、首屏首张案例卡完整可见；卡片网格有清晰层级，页面不出现横向溢出；滚动后所有案例卡和 CTA 可见。
- 移动端 390×844：单列卡片、筛选按钮、标题摘要与详情入口不重叠；键盘/焦点状态可见。
- 分类筛选只改变当前列表，不改变案例事实；空结果有清晰提示或保留“全部”入口。
- 三条现有案例均保留模拟标记；链接指向已存在的 `/polaris`、`/helios`、`/services/socialmedia` 或现有锚点，不产生死链。
- `git diff --check`、针对页面的本地桌面/移动端检查及最终一次 `npm run build` 通过。
- 保留修改前备份，保存实施结果 Markdown；不提交、不推送、不部署，除非后续单独授权。

## 备份与验证方案


- 在第一次写入前备份将受影响的现有数据/组件（至少 `components/LeadtopHomepage.jsx`、相关 CSS，以及新增文件清单）到 `/Users/qianwenyi/Desktop/leadtop-backups/<timestamp>-case-index/`，记录时间、来源和逐文件恢复方式；不覆盖之前备份。
- 基线记录当前 `/cases` 不存在、首页 3 条案例数据、现有详情 URL 和本地构建状态。结果阶段保存页面路由、筛选状态、视口尺寸、溢出和链接检查结果。
- 参考站仅作为布局灵感来源；外部页面内容不直接复制，不把其客户、奖项或数据移植到 Leadtop。

## 授权边界

这是新增页面和新增路由，属于相对上一轮整屏案例区调整的实质范围变化。根据项目 `AGENTS.md`，当前只完成准备；请回复 **开始优化** 后，才进行备份、编码和本地验证。
