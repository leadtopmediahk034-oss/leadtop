# 成功案例宽度与高度调整准备

日期：2026-09-09。当前为 SOP 阶段 9（页面优化）准备状态，适用 Gate 2。延续既有成功案例区任务，仅做局部布局修正；全局 Gate 2 证据本轮未重新核验，不据此授权发布。

## 输入与基线

- 用户截图及要求：成功案例与上方标题区同宽；降低整体高度，使查看切换控件时标题不必被固定导航遮住。
- 复用已有准备与参考调整记录：homepage-success-stories-preparation-2026-09-09.md、homepage-success-stories-reference-adjustment-2026-09-09.md。
- 当前 main 上 JSX/CSS 已有未提交变更；保留现状与此前备份。沿用此前本地预览、不提交的范围。
- 上方 capabilities 使用水平留白 max(20px, calc((100vw - var(--max)) / 2))，其中 --max 为 1680px；成功案例标题仅有 3% 留白，左右边界不一致。
- 轮播舞台桌面高 880px，中等宽度高 920px；桌面图片高 400px，正文最低 300px，加上标题、内边距和控件导致区块过高。
- 固定导航桌面位于 top:18px、高 68px；案例区 scroll-margin-top 为 90px。截图证实查看控件时标题已滚入导航区域，但尚无浏览器实测坐标。

## 拟调整范围

只修改 components/LeadtopHomepage.module.css 中 stories/story 相关规则与对应响应式规则，另保存执行记录。必要 JSX 结构变化须先重新说明范围。

1. 标题容器采用与上方 capabilities 相同的左右边界；轮播内容居中，保留侧面倾斜预览。
2. 收紧标题与操作按钮间距，桌面图片目标约 280–320px，正文按内容自适应；舞台目标约 560–640px，具体根据三个案例的最长正文验证。
3. 缩短卡片与切换控件之间的空白，校准侧卡与指标的位置；为矮屏设置紧凑布局，校准锚点滚动避让距离。
4. 移动端使用自然高度与正常阅读滚动，保证正文、指标、控件互不覆盖。

## 验证、备份和边界

- 执行前保存当前 CSS、JSX 和用户参考图的仓库外时间戳副本到 /Users/qianwenyi/Desktop/leadtop-backups/<timestamp>-stories-layout/，并记录来源、时间及逐文件复制回原路径的恢复方法。保留所有既有备份，不提交备份。
- 基线补测：复用 http://127.0.0.1:3001 与浏览器标签，在桌面 1440×900、1920×1080 和手机 390×844 记录标题边界、舞台/卡片/控件位置；改后同尺寸对比。
- 验收：桌面标题左右边界与上方区块一致；1440×900 下合适滚动位置可同时看到导航下方的标题、活动卡正文和切换控件；无正文裁切、指标重叠及页面横向溢出；三张案例切换正常。移动端正文自然展开、触摸和控件可用。
- 仅局部热更新与对应视口验证，git diff --check；遵守项目最终构建要求，最终修改后至多一次 npm run build。
- 当前基线是代码及截图证据，未证明 SEO、流量或转化改善；验证窗口为本地调整后的同次会话。无论任务是否完成均保留备份。
- 复用现有 React、CSS Modules 与 Phosphor，无依赖安装；本次不修改内容、指标、链接、Meta、Schema 或发布状态。
- 顺序：收到本阶段“开始优化” → 补测并备份 → CSS 局部修改 → 同口径视觉及交互验证 → 记录结果。本次仅准备，代码尚未修改。
