# 首页 Success Stories 重设计准备

## 阶段与授权

- SOP 阶段 9，Gate 2；延续首页案例区任务。用户本轮明确要求先不提交。
- 之前准备范围为删除旧区块及导航依赖处理；新增交互案例区属于实质范围变更，当前仅进行参考研究和准备。全局 Gate 证据本轮未重新核验。
- 使用 redesign-existing-projects 技能，范围限于该区块。

## 参考证据

- 采集日期：2026-09-09；URL：https://www.meetsocial.com/。
- 用户 XPath：`/html/body/div[4]/main/section[6]/div/div[2]`。通过 Success Stories 标题与 DOM 结构定位到第 6 个 section `.ind_r6`，其 `.row_case` 的第二个 div 为 `.ind_r6_con.alanGo`。
- 已观察：中央活动卡片、旋转倾斜的两侧卡片、上图下白色文字面板、区域标签、案例标题、浮动结果指标以及前后切换控件。卡片链接到独立案例页。
- DOM 样式证据：侧卡出现旋转矩阵，活动卡为零旋转；图片和文字面板圆角 16px；浮动指标圆角 8px；转换时长 500ms。
- 标题区含 CASES、Success Stories、成功案例；另有联系我们、更多案例按钮。
- 参考站部分滚动动画导致初始尺寸为 0；滚动后读取到旋转卡片尺寸。截图观察到了区块下缘倾斜卡片；进一步截图工具超时，因此尚未完成居中构图截图核验。

## 拟实现

- 在原位置（capabilities 与 method 之间）增加新的 `section#proof`，保留现有导航和页脚锚点；不再执行此前将导航跳转到产品页的方案。
- 标题使用 Success Stories / 成功案例；延续 Leadtop 橙色、字体和页面底色。
- 使用 Polaris、Helios 两项既有内容和本地图片，呈现中央主卡与侧面倾斜预览，前后按钮切换，支持键盘、移动端手势和减少动态效果偏好。两项内容时仅保留一个有效侧预览，避免伪造额外案例。
- 卡片保留业务类别、简短标题、项目方法链接。原 Polaris 数据明确为模拟数据，不恢复为成功结果；Helios 历史数字缺少本轮原始证据，暂以项目方向文字代替指标。实际客户与成果数据后续须有可追溯来源。
- 点击分别进入 /polaris、/helios；咨询按钮指向 #diagnosis。暂无案例列表页，暂不添加更多案例入口。
- 仅修改 `components/LeadtopHomepage.jsx` 与 `components/LeadtopHomepage.module.css`，另记录本地执行结果。复用 React、CSS Modules、Phosphor；不安装依赖，不改其他区块。

## 基线、备份和验收

- 当前基线：旧区块已经删除；main 上存在未提交首页变更；导航有 5 个 #proof 引用待恢复目标。保留之前的删除前备份。
- 执行前为当前 JSX 与 CSS 创建新的仓库外时间戳备份，位于 `/Users/qianwenyi/Desktop/leadtop-backups/`，记录源、日期与恢复说明。复制备份到源路径可恢复；不覆盖之前备份，不提交备份。
- 验收：新案例区和 5 个锚点有效；前后切换、链接、键盘焦点正常；桌面与移动端仅核查案例区，无横向溢出；差异检查和最后一次构建通过。
- 验证窗口为本次本地执行后；无搜索和转化基线，不声称业务增长改善。备份保留。
- 顺序：范围确认 → 备份 → 局部实现 → 交互及响应式检查 → 最终构建 → 本地预览交付。
- 不提交、不推送、不部署。等待用户对本次新范围输入“开始优化”。
