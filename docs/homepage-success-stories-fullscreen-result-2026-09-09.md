# 成功案例桌面整屏布局结果

日期：2026-09-09。完成用户批准的桌面整屏展示调整。

## 改动

- 仅修改 `components/LeadtopHomepage.module.css` 的成功案例桌面规则。
- 桌面 `.stories` 使用 `min-height: 100dvh`，并以网格行布局把标题、案例舞台和底部切换控件放入同一视口；下一板块不再在案例区底部提前露出。
- 标题与上方内容容器共享 `max(20px, calc((100vw - var(--max)) / 2))` 左右边界；操作按钮置于标题下方。
- 主卡片宽度使用 `clamp(560px, 40vw, 680px)`，图片高度按视口动态放大至最高 480px；矮屏使用紧凑高度，避免正文被挤出视口。
- 保留两侧倾斜案例预览、指标卡、键盘左右切换、移动端自然排版和现有链接。

## 验收

- 1440×900：案例区高度 900px，主卡片 576×590px，标题位于导航下方，切换控件位于底部安全区域；无横向溢出。
- 1920×1080：案例区最小高度 1080px，主卡片按大屏规则放大；下一板块从当前视口底部之后开始。
- 1280×720：矮屏规则生效，案例区仍覆盖 720px 视口，主卡片正文和控件保持可见。
- 390×844：沿用移动端自然流布局，正文、指标和切换控件依次排列，无重叠和横向溢出。
- 连续切换 01→02→03→01 通过；键盘左右切换通过；`overflow: clip` 下区块 `scrollTop` 保持 0。
- `git diff --check` 通过；最新 `npm run build` 成功，19 个页面生成通过。

## 备份与边界

- 新备份：`/Users/qianwenyi/Desktop/leadtop-backups/20260909-161306-stories-fullscreen/`，含修改前 CSS、两张参考图、基线与恢复说明；保留不提交。
- 未修改 JSX、内容、指标、链接、Meta、Schema 或发布状态；未提交、未推送、未部署。
- 本地预览服务继续运行，浏览器临时视口已恢复默认。该结果只说明本地布局改善，不代表 SEO 或业务转化改善。
